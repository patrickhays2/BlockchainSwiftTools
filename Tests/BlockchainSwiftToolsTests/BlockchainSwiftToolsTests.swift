import XCTest
@testable import BlockchainSwiftTools

final class BlockchainSwiftToolsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BlockchainSwiftTools().text, "Hello, World!")
    }
}
